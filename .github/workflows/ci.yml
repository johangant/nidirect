name: CI

on:
  pull_request:
    paths:
      - 'composer.json'
      - 'composer.lock'
      - 'phpcs.xml'
      - 'phpcs.xml.dist'
      - 'phpstan.neon'
      - 'phpstan-disallowed.neon'
      - 'web/modules/**'
      - 'web/themes/**'
      - 'web/profiles/**'
      - '.github/workflows/ci.yml'
  push:
    branches:
      - main
      - development
    paths:
      - 'composer.json'
      - 'composer.lock'
      - 'phpcs.xml'
      - 'phpcs.xml.dist'
      - 'phpstan.neon'
      - 'phpstan-disallowed.neon'
      - 'web/modules/**'
      - 'web/themes/**'
      - 'web/profiles/**'
      - '.github/workflows/ci.yml'

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  static-analysis:
    uses: johangant/webdev-ci/.github/workflows/drupal-static-analysis.yml@github-actions-poc
    with:
      php_version: '8.3'
      coding_standards_dirs: '${{ github.workspace }}/web/modules/custom ${{ github.workspace }}/web/themes/custom'
      deprecated_code_dirs: '${{ github.workspace }}/web/modules/custom ${{ github.workspace }}/web/themes/custom'
      disallowed_functions_dirs: '${{ github.workspace }}/web/modules/custom ${{ github.workspace }}/web/themes/custom'
      ignore_dirs: '/web/themes/custom/nicsdru_nidirect_theme/node_modules'
